# Write a class to hold player information, e.g. what room they are in
# currently.

""""start with player class, 
what it needs to do, 
how it moves around (relate to room.py)
think about what the player is doing (give directions e.g, n, s, e, w)
"""""

class Player:
    def __init__(self, name, current_room):
      self.name = name
      self.current_room = current_room 
    
    # def __direction_(self, direction):
    #     if self.room != None 
    
    